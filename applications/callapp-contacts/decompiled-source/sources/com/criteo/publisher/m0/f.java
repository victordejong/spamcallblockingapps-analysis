package com.criteo.publisher.m0;

import com.criteo.publisher.f.s;
import com.criteo.publisher.f.y;
import com.criteo.publisher.k.a.c;
import com.criteo.publisher.model.b0.m;
import com.criteo.publisher.model.b0.n;
import com.criteo.publisher.model.b0.p;
import com.criteo.publisher.model.b0.r;
import com.criteo.publisher.model.o;
import com.criteo.publisher.model.q;
import com.criteo.publisher.model.v;
import com.criteo.publisher.model.w;
import com.criteo.publisher.model.z;
import com.google.gson.a.a;
import com.google.gson.j;
/* loaded from: classes2-dex2jar.jar:com/criteo/publisher/m0/f.class */
public final class f extends j {
    @Override // com.google.gson.k
    public final <T> j<T> create(com.google.gson.f fVar, a<T> aVar) {
        Class<? super T> rawType = aVar.getRawType();
        if (o.class.isAssignableFrom(rawType)) {
            return (j<T>) o.a(fVar);
        }
        if (q.class.isAssignableFrom(rawType)) {
            return (j<T>) q.a(fVar);
        }
        if (c.class.isAssignableFrom(rawType)) {
            return (j<T>) c.a(fVar);
        }
        if (s.class.isAssignableFrom(rawType)) {
            return (j<T>) s.a(fVar);
        }
        if (y.class.isAssignableFrom(rawType)) {
            return (j<T>) y.a(fVar);
        }
        if (y.a.class.isAssignableFrom(rawType)) {
            return (j<T>) y.a.a(fVar);
        }
        if (y.b.class.isAssignableFrom(rawType)) {
            return (j<T>) y.b.a(fVar);
        }
        if (m.class.isAssignableFrom(rawType)) {
            return (j<T>) m.a(fVar);
        }
        if (n.class.isAssignableFrom(rawType)) {
            return (j<T>) n.a(fVar);
        }
        if (com.criteo.publisher.model.b0.o.class.isAssignableFrom(rawType)) {
            return (j<T>) com.criteo.publisher.model.b0.o.a(fVar);
        }
        if (p.class.isAssignableFrom(rawType)) {
            return (j<T>) p.a(fVar);
        }
        if (com.criteo.publisher.model.b0.q.class.isAssignableFrom(rawType)) {
            return (j<T>) com.criteo.publisher.model.b0.q.a(fVar);
        }
        if (r.class.isAssignableFrom(rawType)) {
            return (j<T>) r.a(fVar);
        }
        if (v.class.isAssignableFrom(rawType)) {
            return (j<T>) v.a(fVar);
        }
        if (w.class.isAssignableFrom(rawType)) {
            return (j<T>) w.a(fVar);
        }
        if (com.criteo.publisher.model.y.class.isAssignableFrom(rawType)) {
            return (j<T>) com.criteo.publisher.model.y.a(fVar);
        }
        if (z.class.isAssignableFrom(rawType)) {
            return (j<T>) z.a(fVar);
        }
        return null;
    }
}
